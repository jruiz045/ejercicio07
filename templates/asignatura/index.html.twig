{% extends 'base.html.twig' %}

{% block title %}Asignatura index{% endblock %}

{% block body %}
    <h1>Asignatura index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Codigo</th>
                <th>Nombre</th>
                <th>NombreIngles</th>
                <th>Credects</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for asignatura in asignaturas %}
            <tr>
                <td>{{ asignatura.id }}</td>
                <td>{{ asignatura.codigo }}</td>
                <td>{{ asignatura.nombre }}</td>
                <td>{{ asignatura.nombreIngles }}</td>
                <td>{{ asignatura.credects }}</td>
                <td>
                    <a href="{{ path('asignatura_show', {'id': asignatura.id}) }}">show</a>
                    <a href="{{ path('asignatura_edit', {'id': asignatura.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="6">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('asignatura_new') }}">Create new</a>
{% endblock %}
